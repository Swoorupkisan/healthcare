services:
  - type: web
    name: healthai
    env: python
    plan: free
    buildCommand: pip install -r requirements-fastapi.txt
    startCommand: python app.py
    envVars:
      - key: GROQ_API_KEY
        scope: run
        isFile: false
